/** @type {import('next').NextConfig} */

const nextConfig = {
	// Export the app as static HTML (required for `next export` with App Router)
	output: 'export',
	// Optionally set a basePath or trailingSlash if needed for Pages hosting
	// trailingSlash: true,
}

module.exports = nextConfig;
